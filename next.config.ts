import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com', 'www.google.com','logo.clearbit.com'],

  },
};

export default nextConfig;
